import {
  NotificationPayload,
  NotificationType,
} from "../providers/notifications";

const notificationFixture: NotificationPayload = {
  message: "MESSAGE",
  type: NotificationType.SUCCESS,
};

export default notificationFixture;
